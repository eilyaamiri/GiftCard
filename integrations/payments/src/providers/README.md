# Concrete provider implementations live here.

# Domain modules (apps/api/src/modules/**) may NOT import from this directory —
# the ESLint boundary rule blocks it. Import the package barrel instead and let
# dependency injection choose the implementation. See AGENTS.md rules 5-7.
