import type { WorkItemType } from "@barat/contracts";

/**
 * Per-type operator guidance.
 *
 * Every work item type shares one backend workspace (one checklist template, one
 * asset record, one send gate), so the type does not change the mechanics — it
 * changes what the operator is supposed to have done before ticking the boxes.
 * Keeping this a `Record<WorkItemType, …>` means a new type fails to compile
 * until someone writes its instructions.
 */
export interface TaskGuidance {
  readonly headline: string;
  readonly steps: readonly string[];
}

export const TASK_GUIDANCE: Record<WorkItemType, TaskGuidance> = {
  MANUAL_GIFT_CARD_FULFILLMENT: {
    headline: "کارت را از تأمین‌کننده بخرید، نتیجه را ثبت کنید و پس از تکمیل چک‌لیست ارسال کنید.",
    steps: [
      "برند، منطقه و مبلغ کارت را با سفارش مطابقت دهید.",
      "خرید را نزد تأمین‌کننده انجام دهید و کد پیگیری آن را بردارید.",
      "نتیجهٔ تأمین‌کننده را با هزینهٔ واقعی ثبت کنید؛ کد فقط همان‌جا وارد می‌شود.",
      "پس از سبز شدن همهٔ موارد چک‌لیست، تحویل را برای مشتری ارسال کنید.",
    ],
  },
  INTERNATIONAL_PAYMENT: {
    headline: "پرداخت را روی سرویس خارجی انجام دهید و رسید آن را ثبت کنید.",
    steps: [
      "شناسهٔ حساب یا شمارهٔ صورتحساب مشتری را بررسی کنید.",
      "پرداخت را روی سرویس خارجی انجام دهید و شمارهٔ پیگیری را بردارید.",
      "هزینهٔ واقعی و رسید تحویل را ثبت کنید.",
      "فعال بودن سرویس را بررسی کنید و سپس تأییدیه را برای مشتری ارسال کنید.",
    ],
  },
  CUSTOMER_INFORMATION: {
    headline: "اطلاعات ناقص سفارش را از مشتری بگیرید تا تحویل ادامه پیدا کند.",
    steps: [
      "مشخص کنید کدام فیلد سفارش ناقص یا نادرست است.",
      "از کانال ثبت‌شدهٔ مشتری اطلاعات درست را درخواست کنید.",
      "پس از اصلاح اطلاعات، کار را تکمیل کنید تا تحویل ادامه یابد.",
    ],
  },
  SUPPLIER_FOLLOWUP: {
    headline: "پاسخ تأمین‌کننده را پیگیری کنید؛ هرگز خرید دوم انجام ندهید.",
    steps: [
      "وضعیت خرید قبلی را نزد تأمین‌کننده استعلام کنید.",
      "نتیجهٔ پیگیری را در یادداشت پایانی کار ثبت کنید.",
      "اگر خرید انجام شده، نتیجهٔ آن را روی همان سفارش ثبت کنید — نه یک خرید تازه.",
    ],
  },
  UNKNOWN_OUTCOME: {
    headline: "نتیجهٔ مبهم تأمین‌کننده را دستی طبقه‌بندی کنید.",
    steps: [
      "پیش از هر اقدامی بررسی کنید که آیا خرید واقعاً انجام شده است یا نه.",
      "اگر انجام شده، نتیجه را ثبت کنید تا سفارش بسته شود.",
      "اگر انجام نشده، کار را با توضیح ناموفق علامت بزنید تا بازگشت وجه بررسی شود.",
    ],
  },
  REFUND_REVIEW: {
    headline: "درخواست بازگشت وجه را بررسی کنید؛ تصمیم مالی نزد نقش‌های مالی است.",
    steps: [
      "بررسی کنید که کد یا سرویس واقعاً به مشتری نرسیده باشد.",
      "دلیل تصمیم را در یادداشت پایانی کار ثبت کنید.",
      "اجرای بازگشت وجه از پنل مالی انجام می‌شود، نه از این صفحه.",
    ],
  },
  SUPPORT_REQUEST: {
    headline: "به پرسش مشتری پاسخ دهید و کار را ببندید.",
    steps: [
      "سابقهٔ سفارش را بررسی کنید.",
      "پاسخ را از کانال پشتیبانی برای مشتری بفرستید.",
      "خلاصهٔ پاسخ را در یادداشت پایانی کار ثبت کنید.",
    ],
  },
};
